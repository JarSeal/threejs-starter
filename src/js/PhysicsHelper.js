class PhysicsHelper {
    constructor(scene) {
        // This class is for debugging the physical elements' physical positions on the screen.
        // Usually it is shown as a wireframe object.
        this.scene = scene;
    }

    update() {
        // Update the helper positions here (implement here)
    }

    addVisual(body, color) {
        body, color;
        // Add a visual helper object to the scene here (implement here)
    }
}

export default PhysicsHelper;